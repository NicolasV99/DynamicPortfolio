<%- include('partials/header') %>

<section class="container">
    <h1>Mis Proyectos</h1>
    <p>Estos proyectos se generan dinámicamente desde el servidor (Server-Side Rendering).</p>

    <div class="project-grid">
        <% projectList.forEach(function(project) { %>
            
            <div class="card">
                <h3><%= project.title %></h3>
                <p><%= project.description %></p>
                <small>ID: <%= project.id %></small>
            </div>

        <% }); %>
        </div>
</section>

<style>
    .project-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-top: 20px; }
    .card { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); border-left: 5px solid #007BFF; }
</style>

<%- include('partials/footer') %>